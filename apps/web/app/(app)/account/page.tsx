import { AccountPageClient } from "./_components/AccountPageClient";

export const metadata = { title: "Account — StudyForge" };

export default function AccountPage() {
  return <AccountPageClient />;
}
